# Server Configuration
NODE_ENV=development
PORT=3000
HOST=0.0.0.0

# Supabase Configuration
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=your-supabase-anon-key
SUPABASE_SERVICE_KEY=your-supabase-service-role-key

# Redis Configuration (use 127.0.0.1 on Windows to avoid IPv6 ::1 issues)
# Start Redis: Docker -> docker run -d -p 6379:6379 redis:alpine
# Or install Redis for Windows / WSL and run: redis-server
REDIS_URL=redis://127.0.0.1:6379

# Queue concurrency (optional; defaults: agent_tasks=6, build=6, publish=2)
# AGENT_TASKS_CONCURRENCY=6
# BUILD_CONCURRENCY=6
# PUBLISH_CONCURRENCY=2

# Gemini AI Configuration
GEMINI_API_KEY=your-gemini-api-key

# Google OAuth (for Google Search Console integration)
# Redirect URI must point to THIS server so the OAuth callback can save the integration to the DB.
# Add this EXACT URI in Google Cloud Console → Credentials → OAuth 2.0 Client → Authorized redirect URIs (no trailing slash).
GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-google-client-secret
GOOGLE_REDIRECT_URI=http://localhost:3000/api/integrations/gsc/callback

# JWT Secret (for token signing)
JWT_SECRET=development-secret-key

# Application URLs
FRONTEND_URL=http://localhost:5173
API_URL=http://localhost:3000

# Webhooks
WEBHOOK_SECRET=your-webhook-secret
WEBHOOK_IP_WHITELIST=
